object TiposTest {
    def main(args: Array[String]): Unit = {

      /*
      En Scala tenemos una raiz de tipos que serian los Any, de este derivan los AnyVal y los AnyRef
         Tenemos varios tipos de AnyVal como:
            Int, Long Double, Float, Byte, Short, Char, Boolean, Unit
         
         Aunque tambien tenemos otros tipos como Null, pero no es muy recomendado utilizarlos
         Nothing es de todos los tipos a la vez, no podemos crearlos, ni manipularlos, esto solo se utiliza para expresiones que no devulven nada.
         Los creadores del lenguaje lo recomiendan en algunos casos porque es mas amigable con la maquina virtual.
      */

    }
}
